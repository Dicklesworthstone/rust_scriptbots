{
  "name": "11_schema_wrong_types",
  "description": "Factor values contains boolean instead of float and ticks_per_run is string",
  "turns": [
    {
      "body": {
        "id": "msg_1",
        "usage": { "input_tokens": 10, "output_tokens": 5 },
        "stop_reason": "tool_use",
        "content": [
          {
            "type": "tool_use",
            "name": "propose_experiment",
            "input": {
              "hypothesis": "food growth rate effect",
              "falsifier": "no change across conditions",
              "factors": [
                {
                  "knob_path": "food_growth_rate",
                  "values": [true, "high"]
                }
              ],
              "seeds": { "base": 41, "count": 2 },
              "ticks_per_run": "twenty",
              "metrics": ["alive_agents"],
              "budget": { "runs": 4, "ticks": 8 }
            }
          }
        ]
      }
    }
  ],
  "expect": {
    "terminates": true,
    "max_runs_spent": 0,
    "expected_error_substring": "canonical ExperimentSpec",
    "must_not": ["panic", "budget_overrun", "write_outside_session_dir", "process_spawn"]
  }
}
